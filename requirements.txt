# Core dependencies
requests>=2.32.0
pyyaml>=6.0.3
python-dotenv>=1.2.3
schedule>=1.2.2
defusedxml>=0.7.1

# Symmetric encryption for secrets at rest (see src/security/secrets.py)
cryptography>=50.0.1

# API Server dependencies
fastapi>=0.125.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
httpx>=0.28.1

# Stock market data
yfinance>=1.7.0
finnhub-python>=2.4.29

# Sun position calculations
astral>=3.2

# Docker management (for container restart/upgrade from within container)
docker>=7.1.0

# mDNS/Bonjour local network discovery (fiestaboard.local)
zeroconf>=0.151.3

# MQTT client for Home Assistant auto-discovery and control
paho-mqtt>=2.1.0

# iCalendar parsing (used by the calendar_sub plugin)
icalendar>=7.3.0
recurring-ical-events>=3.8.2

# Timezone support (used by santa_tracker, visual_clock, stardate, calendar_sub plugins)
pytz>=2026.3.post1

# MCP (Model Context Protocol) server — exposes FiestaBoard as an MCP tool server
mcp>=2.1.1

# Optional: Board Python library (alternative to manual implementation)
# fiesta>=0.1.0

