{
  "id": "sun_art",
  "name": "Sun Art",
  "version": "1.0.0",
  "description": "Display a full-screen sun art pattern that changes based on the sun's position throughout the day",
  "author": "FiestaBoard Team",
  "repository": "https://github.com/FiestaBoard/FiestaBoard",
  "documentation": "README.md",
  "icon": "sun",
  "category": "weather",

  "settings_schema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "title": "Enable Sun Art",
        "default": false
      },
      "latitude": {
        "type": "number",
        "title": "Latitude",
        "description": "Location latitude for sun calculations (-90 to 90)",
        "minimum": -90,
        "maximum": 90
      },
      "longitude": {
        "type": "number",
        "title": "Longitude",
        "description": "Location longitude for sun calculations (-180 to 180)",
        "minimum": -180,
        "maximum": 180
      },
      "refresh_seconds": {
        "type": "integer",
        "title": "Refresh Interval (seconds)",
        "description": "How often to recalculate sun position and update pattern (minimum 60 seconds)",
        "default": 300,
        "minimum": 60
      }
    },
    "required": ["latitude", "longitude"]
  },

  "env_vars": [
    {
      "name": "SUN_ART_ENABLED",
      "required": false,
      "description": "Enable sun art feature",
      "default": "false"
    },
    {
      "name": "SUN_ART_LATITUDE",
      "required": false,
      "description": "Location latitude (-90 to 90)"
    },
    {
      "name": "SUN_ART_LONGITUDE",
      "required": false,
      "description": "Location longitude (-180 to 180)"
    },
    {
      "name": "SUN_ART_REFRESH_SECONDS",
      "required": false,
      "description": "Refresh interval in seconds (default: 300)",
      "default": "300"
    }
  ],

  "variables": {
    "simple": [
      "sun_art",
      "sun_stage",
      "sun_position",
      "is_daytime",
      "time_to_sunrise",
      "time_to_sunset"
    ]
  },

  "max_lengths": {
    "sun_art": 200,
    "sun_stage": 15,
    "sun_position": 6,
    "is_daytime": 1,
    "time_to_sunrise": 8,
    "time_to_sunset": 8
  }
}
