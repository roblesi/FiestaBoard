# Development dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0
pytest-asyncio>=0.24.0
pytest-xdist>=3.6.0
coverage>=7.6.0

# Linting
# Pinned exactly: ruff's formatter output changes between releases (e.g. 0.16.0
# began reformatting code blocks inside Markdown), so an unpinned range makes
# `ruff format --check` in CI nondeterministic — a new release can turn CI red
# on unchanged code. Bump this deliberately and reformat in the same commit.
ruff==0.16.6

